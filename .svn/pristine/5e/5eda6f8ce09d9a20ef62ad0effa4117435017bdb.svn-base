# 在spring 4.x中已经不支持7个参数的crontab表达式了，要求必须是6个参数。cron表达式的格式
# {秒} {分} {时} {日期（具体哪天）} {月} {星期}
# 秒：必填项，允许的值范围是0-59，支持的特殊符号包括, - * /，,表示特定的某一秒才会触发任务，-表示一段时间内会触发任务，*表示每一秒都会触发，/表示从哪一个时刻开始，每隔多长时间触发一次任务。
# 分：必填项，允许的值范围是0-59，支持的特殊符号和秒一样，含义类推
# 时：必填项，允许的值范围是0-23，支持的特殊符号和秒一样，含义类推
# 日期：必填项，允许的值范围是1-31，支持的特殊符号相比秒多了?，表示与{星期}互斥，即意味着若明确指定{星期}触发，则表示{日期}无意义，以免引起冲突和混乱。
# 月：必填项，允许的值范围是1-12（JAN-DEC），支持的特殊符号与秒一样，含义类推
# 星期：必填项，允许值范围是1~7 (SUN-SAT),1代表星期天（一星期的第一天），以此类推，7代表星期六，支持的符号相比秒多了?，表达的含义是与{日期}互斥，即意味着若明确指定{日期}触发，则表示{星期}无意义

# 中天三级告警短信放策略：
# 告警金额=0表示不发送告警短信，中天告警分三级告警，哪一级为0表示那一级不发送告警短信
# 告警级别2  < 剩余金额 < 告警级别1 时，发送一次告警短信即可
# 告警级别3  < 剩余金额 <= 告警级别2 时，发送一次告警短信即可
# 剩余金额 <= 告警级别3 时，每天都发送一次告警短信，一天之内不发送，最多发送多少次可以配置

# job开关，1表示打开，0表示关闭
job.isOpen=0

# 定时任务1配置
# 每天早上8点到晚上8点，每隔30分钟执行一次
job1.schedule = 0 */30 8-20 * * *

# 每天凌晨1点到早上9点，每隔2个小时执行一次，从第1分钟开始
#job1.schedule = 0 1 1-9/2 * * *

# 定时任务2配置
# 每天早上8点到晚上8点，从第2分钟开始，每隔30分钟执行一次
job2.schedule = 0 2/30 8-20 * * *

# 每天凌晨1点到早上9点，每隔2个小时执行一次，从第3分钟开始
#job2.schedule = 0 3 1-9/2 * * *

# 定时任务3配置
# 每天早上8点到晚上8点，从第4分钟开始，每隔30分钟执行一次
job3.schedule = 0 4/30 8-20 * * *

# 每天凌晨1点到早上9点，每隔2个小时执行一次，从第5分钟开始
#job3.schedule = 0 5 1-9/2 * * *

# 最多发送次数,如果为0，表示没有次数限制
job3.num=0

# 1天后在发送
job3.time=86400

# 检查短信发送状态定时任务配置
# 每天早上8点到晚上8点，从第15分钟开始，每隔30分钟执行一次
job4.schedule = 0 15/30 8-20 * * *

# 检查提现失败记录定时任务配置
# 每隔30分钟执行一次
job5.schedule = 0 */30 * * * *

# 检查分账失败记录定时任务配置
# 每天凌晨2点执行
job6.schedule = 0 0 2 * * ?

